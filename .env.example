# API keys are entered in the browser and kept in sessionStorage for the current tab.
# No server-side API key is required for the current client-key flow.
NEXT_PUBLIC_APP_NAME=IITP 평가관리
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=your-supabase-publishable-key
